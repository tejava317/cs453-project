import { MetricManager } from "@syntest/metric";
import { Plugin } from "../Plugin";
export declare abstract class EventListenerPlugin extends Plugin {
    constructor(name: string, describe: string);
    abstract setupEventListener(metricManager: MetricManager): Promise<void> | void;
}
//# sourceMappingURL=EventListenerPlugin.d.ts.map