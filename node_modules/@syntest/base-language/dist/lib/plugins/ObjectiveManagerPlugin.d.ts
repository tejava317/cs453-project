import { Plugin } from "@syntest/module";
import { Encoding, EncodingRunner, ObjectiveManager, SecondaryObjectiveComparator } from "@syntest/search";
export type ObjectiveManagerOptions<T extends Encoding> = {
    runner: EncodingRunner<T>;
    secondaryObjectives: Set<SecondaryObjectiveComparator<T>>;
};
export declare abstract class ObjectiveManagerPlugin<T extends Encoding> extends Plugin {
    constructor(name: string, describe: string);
    abstract createObjectiveManager<O extends ObjectiveManagerOptions<T>>(options: O): ObjectiveManager<T>;
    getOptionChoices(option: string): string[];
}
//# sourceMappingURL=ObjectiveManagerPlugin.d.ts.map