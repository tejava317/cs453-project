import { Plugin } from "@syntest/module";
import { Encoding, EncodingSampler, SearchSubject } from "@syntest/search";
export type SamplerOptions<T extends Encoding> = {
    subject: SearchSubject<T>;
};
export declare abstract class SamplerPlugin<T extends Encoding> extends Plugin {
    constructor(name: string, describe: string);
    abstract createSamplerOperator<O extends SamplerOptions<T>>(options: O): EncodingSampler<T>;
    getOptionChoices(option: string): string[];
}
//# sourceMappingURL=SamplerPlugin.d.ts.map