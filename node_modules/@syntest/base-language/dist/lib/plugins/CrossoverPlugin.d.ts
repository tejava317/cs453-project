import { Plugin } from "@syntest/module";
import { Crossover, Encoding } from "@syntest/search";
export type CrossoverOptions = {
    crossoverEncodingProbability: number;
    crossoverStatementProbability: number;
};
export declare abstract class CrossoverPlugin<T extends Encoding> extends Plugin {
    constructor(name: string, describe: string);
    abstract createCrossoverOperator<O extends CrossoverOptions>(options: O): Crossover<T>;
    getOptionChoices(option: string): string[];
}
//# sourceMappingURL=CrossoverPlugin.d.ts.map