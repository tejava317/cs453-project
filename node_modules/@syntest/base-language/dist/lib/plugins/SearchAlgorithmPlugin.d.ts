import { Plugin } from "@syntest/module";
import { Encoding, EncodingSampler, ObjectiveManager, Procreation, SearchAlgorithm } from "@syntest/search";
export type SearchAlgorithmOptions<T extends Encoding> = {
    objectiveManager: ObjectiveManager<T>;
    encodingSampler: EncodingSampler<T>;
    procreation: Procreation<T>;
    populationSize: number;
};
export declare abstract class SearchAlgorithmPlugin<T extends Encoding> extends Plugin {
    constructor(name: string, describe: string);
    abstract createSearchAlgorithm<O extends SearchAlgorithmOptions<T>>(options: O): SearchAlgorithm<T>;
    getOptionChoices(option: string): string[];
}
//# sourceMappingURL=SearchAlgorithmPlugin.d.ts.map