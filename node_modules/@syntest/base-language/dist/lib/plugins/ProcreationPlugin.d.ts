import { Plugin } from "@syntest/module";
import { Crossover, Encoding, EncodingSampler, Procreation } from "@syntest/search";
export type ProcreationOptions<T extends Encoding> = {
    crossover: Crossover<T>;
    mutateFunction: (sampler: EncodingSampler<T>, encoding: T) => T;
    sampler: EncodingSampler<T>;
};
export declare abstract class ProcreationPlugin<T extends Encoding> extends Plugin {
    constructor(name: string, describe: string);
    abstract createProcreationOperator<O extends ProcreationOptions<T>>(options: O): Procreation<T>;
    getOptionChoices(option: string): string[];
}
//# sourceMappingURL=ProcreationPlugin.d.ts.map