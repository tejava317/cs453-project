import { UserInterface } from "@syntest/cli-graphics";
import { MetricManager } from "@syntest/metric";
import { ModuleManager } from "@syntest/module";
import { StorageManager } from "@syntest/storage";
import { ArgumentsObject } from "./Configuration";
export declare abstract class Launcher {
    protected arguments_: ArgumentsObject;
    protected moduleManager: ModuleManager;
    protected metricManager: MetricManager;
    protected storageManager: StorageManager;
    protected userInterface: UserInterface;
    constructor(arguments_: ArgumentsObject, moduleManager: ModuleManager, metricManager: MetricManager, storageManager: StorageManager, userInterface: UserInterface);
    run(): Promise<void>;
    protected registerProperties(): void;
    abstract initialize(): Promise<void>;
    abstract preprocess(): Promise<void>;
    abstract process(): Promise<void>;
    abstract postprocess(): Promise<void>;
    abstract exit(): Promise<void>;
}
//# sourceMappingURL=Launcher.d.ts.map