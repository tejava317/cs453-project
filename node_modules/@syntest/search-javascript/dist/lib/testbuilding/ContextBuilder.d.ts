import { Export } from "@syntest/analysis-javascript";
import { Logger } from "@syntest/logging";
import { Statement } from "../testcase/statements/Statement";
type Require = {
    left: string;
    right: string;
};
export declare class ContextBuilder {
    protected static LOGGER: Logger;
    private targetRootDirectory;
    private sourceDirectory;
    private globalNameCount;
    private testNameCount;
    private imports;
    private statementVariableNameMap;
    constructor(targetRootDirectory: string, sourceDirectory: string);
    nextTestCase(): void;
    getOrCreateVariableName(statement: Statement): string;
    getOrCreateImportName(export_: Export): string;
    private _addImport;
    private _getImportString;
    private _getRequireString;
    getImports(assertionsPresent: boolean): {
        imports: string[];
        requires: Require[];
    };
}
export {};
//# sourceMappingURL=ContextBuilder.d.ts.map