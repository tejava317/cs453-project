import { ContextBuilder } from "../../../testbuilding/ContextBuilder";
import { JavaScriptTestCaseSampler } from "../../sampling/JavaScriptTestCaseSampler";
import { Decoding, Statement } from "../Statement";
/**
 * @author Dimitri Stallenberg
 */
export declare class ArrayStatement extends Statement {
    private _elements;
    constructor(variableIdentifier: string, typeIdentifier: string, name: string, uniqueId: string, elements: Statement[]);
    mutate(sampler: JavaScriptTestCaseSampler, depth: number): Statement;
    copy(): ArrayStatement;
    decode(context: ContextBuilder): Decoding[];
    getChildren(): Statement[];
    hasChildren(): boolean;
    setChild(index: number, newChild: Statement): void;
    protected get children(): Statement[];
}
//# sourceMappingURL=ArrayStatement.d.ts.map