import { Export } from "@syntest/analysis-javascript";
import { ContextBuilder } from "../../../testbuilding/ContextBuilder";
import { JavaScriptTestCaseSampler } from "../../sampling/JavaScriptTestCaseSampler";
import { Decoding, Statement } from "../Statement";
import { ActionStatement } from "./ActionStatement";
/**
 * ConstructorCall
 */
export declare class ConstructorCall extends ActionStatement {
    private _classIdentifier;
    get classIdentifier(): string;
    /**
     * Constructor
     * @param type the return identifierDescription of the constructor
     * @param uniqueId optional argument
     * @param args the arguments of the constructor
     */
    constructor(variableIdentifier: string, typeIdentifier: string, classIdentifier: string, name: string, uniqueId: string, arguments_: Statement[], export_: Export);
    mutate(sampler: JavaScriptTestCaseSampler, depth: number): ConstructorCall;
    copy(): ConstructorCall;
    decode(context: ContextBuilder): Decoding[];
}
//# sourceMappingURL=ConstructorCall.d.ts.map