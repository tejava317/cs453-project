import { Export, TypeEnum } from "@syntest/analysis-javascript";
import { Encoding, EncodingSampler } from "@syntest/search";
import { Statement } from "../Statement";
/**
 * ActionStatement
 */
export declare abstract class ActionStatement extends Statement {
    private _args;
    protected _export?: Export;
    protected constructor(variableIdentifier: string, typeIdentifier: string, name: string, ownType: TypeEnum, uniqueId: string, arguments_: Statement[], export_?: Export);
    abstract mutate(sampler: EncodingSampler<Encoding>, depth: number): ActionStatement;
    abstract copy(): ActionStatement;
    setChild(index: number, newChild: Statement): void;
    hasChildren(): boolean;
    getChildren(): Statement[];
    protected get args(): Statement[];
    get export(): Export;
}
//# sourceMappingURL=ActionStatement.d.ts.map