import { Export } from "@syntest/analysis-javascript";
import { ContextBuilder } from "../../../testbuilding/ContextBuilder";
import { JavaScriptTestCaseSampler } from "../../sampling/JavaScriptTestCaseSampler";
import { Decoding, Statement } from "../Statement";
import { ActionStatement } from "./ActionStatement";
/**
 * @author Dimitri Stallenberg
 */
export declare class FunctionCall extends ActionStatement {
    /**
     * Constructor
     * @param uniqueId id of the gene
     * @param functionName the name of the function
     * @param args the arguments of the function
     */
    constructor(variableIdentifier: string, typeIdentifier: string, name: string, uniqueId: string, arguments_: Statement[], export_: Export);
    mutate(sampler: JavaScriptTestCaseSampler, depth: number): FunctionCall;
    copy(): FunctionCall;
    decode(context: ContextBuilder): Decoding[];
}
//# sourceMappingURL=FunctionCall.d.ts.map