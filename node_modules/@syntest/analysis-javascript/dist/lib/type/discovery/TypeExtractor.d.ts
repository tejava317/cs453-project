import * as t from "@babel/types";
import { Factory } from "../../Factory";
export declare class TypeExtractor extends Factory {
    extractElements(filepath: string, ast: t.Node): Map<string, import("./element/Element").Element>;
    extractRelations(filepath: string, ast: t.Node): Map<string, import("./relation/Relation").Relation>;
    extractObjectTypes(filepath: string, ast: t.Node): Map<string, import("./object/DiscoveredType").DiscoveredObjectType>;
}
//# sourceMappingURL=TypeExtractor.d.ts.map